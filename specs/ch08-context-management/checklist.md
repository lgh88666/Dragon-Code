# ch08：上下文管理 Checklist

## 状态

- 阶段：已完成并验收（2026-08-08）。
- 上游文档：`spec.md`、`plan.md`、`task.md` 已批准；四份文档已全部通过审批。
- 使用规则：只有取得命令输出、断言结果或 tmux 观察证据后才能勾选。

## A. 配置与兼容性

- [x] **A1**：旧 Provider 配置不包含 `context_window` 和 `summary_model` 时仍能加载并启动。
  - 验证：现有配置测试全部通过；构造旧 YAML 后 `load_config()` 成功。
- [x] **A2**：Anthropic 未配置窗口时得到 200000。
  - 验证：单元测试断言解析后的值。
- [x] **A3**：OpenAI 未配置窗口时得到 128000。
  - 验证：单元测试断言解析后的值。
- [x] **A4**：显式正整数 `context_window` 覆盖协议默认值。
  - 验证：分别解析 80000、256000。
- [x] **A5**：非法 `context_window` 被拒绝。
  - 验证：0、负数、布尔值、浮点数、字符串均抛出可读 ConfigError。
- [x] **A6**：`summary_model` 省略时回退主模型，配置后使用该模型。
  - 验证：fake client factory 分别观察模型名。
- [x] **A7**：摘要 Client 与主 Client 复用协议、API Key 和 base_url，但属于不同对象。
  - 验证：TUI/工厂单元测试检查两次构造参数，不打印密钥。
- [x] **A8**：示例 YAML 包含两个新字段及可选说明，并能被 `yaml.safe_load` 解析。
  - 验证：解析命令退出码为0。

## B. 会话目录与路径安全

- [x] **B1**：每次 Agent 会话生成唯一 `<unix_ts>-<8hex>` ID。
  - 验证：连续生成多次均匹配格式且不重复。
- [x] **B2**：工具结果目录位于当前项目 `.dragon-code/sessions/<id>/tool-results/`。
  - 验证：路径对象解析后仍位于 working_dir 内。
- [x] **B3**：调用 ID 含 `/`、`\`、`..`、冒号和 Windows 保留字符时不能逃逸目录。
  - 验证：路径安全单元测试。
- [x] **B4**：清洗前缀相同但原 ID 不同的结果文件不碰撞。
  - 验证：哈希后缀不同。
- [x] **B5**：`.dragon-code/sessions/` 被 Git 忽略，配置示例仍可跟踪。
  - 验证：`git check-ignore`。
- [x] **B6**：正常退出后会话目录和结果文件继续存在。
  - 验证：tmux 退出后 `Test-Path`/`ls`。

## C. 单条工具结果预防性压缩

- [x] **C1**：49999和50000字节结果不因“超过50000”规则被替换，50001字节结果被替换。
  - 验证：参数化边界测试。
- [x] **C2**：字节数按 UTF-8 计算，中文不按 Python 字符数误判。
  - 验证：中文字符串的字符数小于阈值但编码字节数超过阈值时触发。
- [x] **C3**：完整原文逐字节保存到结果文件。
  - 验证：`read_bytes()` 等于原始 `content.encode("utf-8")`。
- [x] **C4**：预览包含原始字节数、头部、保存路径、调用 ID和 Read 重读提示。
  - 验证：逐项字符串断言。
- [x] **C5**：预览头部不超过20行且不超过2048 UTF-8字节。
  - 验证：长行、多行和中文边界测试。
- [x] **C6**：字节截断后仍可 UTF-8 解码，不产生替换字符。
  - 验证：在中文多字节中间达到限制的测试。
- [x] **C7**：替换后的 ToolResult 保留 call_id、tool_name、success、error、metadata 语义。
  - 验证：成功与失败 ToolResult 对比。
- [x] **C8**：处理函数返回新对象，不修改调用方原 ToolResult。
  - 验证：处理后原对象 content 仍为完整原文。

## D. 单轮聚合与冻结账本

- [x] **D1**：单轮未超过200000字节时不做额外聚合替换。
  - 验证：多个小结果合计正好200000。
- [x] **D2**：五个各45000字节结果只替换一个即可回落至阈值。
  - 验证：正好一个结果落盘。
- [x] **D3**：不同大小按从大到小替换，达到预算后立即停止。
  - 验证：构造可识别内容并检查选择集合。
- [x] **D4**：大小相同时按原模型调用顺序选择。
  - 验证：重复运行结果一致。
- [x] **D5**：单条规则已替换的结果不重复计入聚合候选。
  - 验证：混合50KB以上和以下结果。
- [x] **D6**：同一调用 ID 再次处理不重写文件，mtime 不变。
  - 验证：两次处理前后 `st_mtime_ns` 相同。
- [x] **D7**：保留决定冻结后，即使后续聚合环境变化也不翻转。
  - 验证：同一 ID 先单独保留，再放入超预算轮次仍保留。
- [x] **D8**：替换决定冻结后始终复用逐字节相同预览。
  - 验证：多轮 `content == first_preview`。
- [x] **D9**：模拟落盘失败时保留原文、不写账本、不留下最终文件。
  - 验证：monkeypatch 写入异常。
- [x] **D10**：落盘恢复后同一 ID 可以重新评估并成功替换。
  - 验证：失败后第二次调用成功。

## E. 工具层完整结果

- [x] **E1**：Read 超过旧2000行/100000字符限制仍返回完整带行号内容。
  - 验证：工具单元测试比对末行。
- [x] **E1a**：Read 使用 `offset`/`limit` 能分段读取落盘文件尾部，默认读取行为不变。
  - 验证：500行文件读取451–500行；真实 DeepSeek 重读验收文件尾部标记。
- [x] **E2**：Bash 超过旧100000字符限制仍返回完整 stdout/stderr。
  - 验证：尾部标记存在。
- [x] **E3**：Glob 超过旧200项限制仍返回完整排序结果。
  - 验证：首尾路径与总数。
- [x] **E4**：Grep 超过旧200项限制仍返回全部命中，单行异常长度保护不破坏结果顺序。
  - 验证：命中数和尾部命中。
- [x] **E5**：MCP 超过旧100000字符限制仍返回完整内容和 structuredContent。
  - 验证：fake MCP result 尾部标记存在。
- [x] **E6**：上述大结果进入 Agent 后会立刻由统一管理器落盘，Conversation 不保存完整巨量文本。
  - 验证：Agent 集成测试。

## F. Token 估算与 usage 锚点

- [x] **F1**：无锚点时按完整 system、messages、tools、reminder 字符量除以3.5向上取整。
  - 验证：固定fixture精确断言。
- [x] **F2**：工具名称、描述和 JSON Schema 被计入估算。
  - 验证：增加大 schema 后估算上升。
- [x] **F3**：最新主请求 usage 替换旧锚点，不跨请求累加。
  - 验证：连续返回1000、1500、2200时锚点依次等于最新值。
- [x] **F4**：Assistant输出已包含在usage中，不被下一轮字符增量重复计算。
  - 验证：记录响应后立即估算无重复增长。
- [x] **F5**：响应后新增 Tool Result 被计入下一轮增量。
  - 验证：追加大工具结果后估算增加。
- [x] **F6**：摘要请求 usage 不更新主锚点。
  - 验证：fake摘要返回不同usage，锚点保持主请求值。
- [x] **F7**：历史压缩或字符量下降后旧锚点失效，回退全量估算。
  - 验证：替换短历史后结果按短历史计算。
- [x] **F8**：取消或失败的主请求不更新锚点。
  - 验证：异常与CancelledError用例。

## G. 摘要 Prompt、解析与近期原文

- [x] **G1**：摘要请求 tools 为空，reminder 为空，使用摘要专用 SystemPrompt。
  - 验证：fake client 捕获 LLMRequest。
- [x] **G2**：Prompt 首尾均明确禁止调用工具。
  - 验证：Prompt 文本断言。
- [x] **G3**：Prompt 要求 `<analysis>` 和九部分 `<summary>`。
  - 验证：九个标题全部出现。
- [x] **G4**：只保存唯一非空 `<summary>`，分析草稿被丢弃。
  - 验证：解析测试。
- [x] **G5**：缺失、空白或歧义多个 summary 标签时失败且不修改历史。
  - 验证：参数化解析失败。
- [x] **G6**：摘要模型返回 ToolCall 时视为失败。
  - 验证：fake流产生工具调用。
- [x] **G7**：近期原文同时满足不少于10000 Token和不少于5条消息。
  - 验证：一个边界先满足、另一个后满足的两组测试。
- [x] **G8**：历史总量不足两个下界时保留全部。
  - 验证：短历史测试。
- [x] **G9**：Assistant ToolCall和对应Tool Result不会被截断边界拆开。
  - 验证：边界正好落在二者中间。
- [x] **G10**：新历史为摘要/边界 user 消息后追加近期原文，原输入对象不被修改。
  - 验证：深拷贝与顺序断言。

## H. 自动压缩与熔断

- [x] **H1**：估算低于 `context_window - 33000` 时不调用摘要 Client。
  - 验证：阈值减1。
- [x] **H2**：达到阈值时在主模型请求前调用摘要 Client。
  - 验证：调用时间顺序记录。
- [x] **H3**：自动成功后 Conversation 原子替换，主任务使用新历史继续。
  - 验证：Agent fake主模型观察压缩历史。
- [x] **H4**：当前尚未提交的用户消息不进入摘要，压缩后仍逐字保持在请求尾部。
  - 验证：首轮触发压缩并让主请求失败，Conversation仍无该用户消息。
- [x] **H5**：自动失败时原历史不变，并继续发主请求。
  - 验证：摘要异常后主fake client仍被调用。
- [x] **H6**：自动成功清零此前1或2次连续失败。
  - 验证：失败、失败、成功序列。
- [x] **H7**：第三次连续自动失败进入熔断并产生提示事件。
  - 验证：事件phase和计数。
- [x] **H8**：熔断后达到阈值也不再调用摘要 Client。
  - 验证：调用次数保持3。
- [x] **H9**：新建会话时熔断状态恢复为未触发。
  - 验证：新 ContextManager 实例。
- [x] **H10**：取消摘要不计入失败次数。
  - 验证：CancelledError传播且计数不变。

## I. 手动 `/compact` 与 TUI

- [x] **I1**：低Token历史输入 `/compact` 仍调用一次摘要。
  - 验证：TUI+Agent测试。
- [x] **I2**：熔断后 `/compact` 仍可调用摘要。
  - 验证：手动调用次数增加。
- [x] **I3**：手动成功/失败均不修改自动熔断计数。
  - 验证：前后计数相同。
- [x] **I4**：预计摘要输入超过 `context_window - 23000` 时安全失败，不先修改历史。
  - 验证：小窗口测试。
- [x] **I5**：`/compact` 不进入 Conversation，不触发普通对话请求。
  - 验证：历史和主 client 调用次数。
- [x] **I6**：成功文案包含 before 和 after Token，且 before > after 的fixture正确显示。
  - 验证：TUI文本断言。
- [x] **I7**：失败文案安全可读，不包含 SDK 原始请求或密钥。
  - 验证：注入带伪密钥异常后断言未出现。
- [x] **I8**：`/help` 显示 `/compact`。
  - 验证：TUI测试。
- [x] **I9**：Agent运行中输入仍禁用，不会与手动压缩并发。
  - 验证：慢fake client期间状态断言。
- [x] **I10**：Escape取消摘要后恢复IDLE和输入焦点，不计入熔断。
  - 验证：Textual pilot测试。
- [x] **I11**：自动压缩开始、成功、失败和熔断事件都使用统一格式化路径。
  - 验证：事件与显示文案测试。

## J. Agent、权限、模式和协议回归

- [x] **J1**：ToolScheduler并发/串行分批规则不变。
  - 验证：原调度测试全部通过。
- [x] **J2**：PermissionEngine询问、允许本次、本会话、永久和拒绝行为不变。
  - 验证：权限与Agent原测试通过。
- [x] **J3**：工具结果经过落盘后仍按模型原始调用顺序回灌。
  - 验证：混合并发/串行Agent测试。
- [x] **J4**：TUI tool_end内容与Conversation保存内容逐字相同。
  - 验证：Agent集成断言。
- [x] **J5**：Plan Mode使用Read/Glob/Grep定义，Default使用完整定义；同轮只计算一次。
  - 验证：带计数Registry测试。
- [x] **J6**：Anthropic隐藏thinking块和tool_result配对在压缩前后仍合法。
  - 验证：Anthropic client回归测试。
- [x] **J7**：OpenAI tool_calls参数分片和tool角色结果在压缩前后仍合法。
  - 验证：OpenAI client回归测试。
- [x] **J8**：MCP工具权限和结果适配不因上下文管理改变。
  - 验证：MCP及Agent测试。
- [x] **J9**：未触发压缩的普通聊天、工具循环、取消和50轮上限行为不变。
  - 验证：完整原测试套件。

## K. 质量门槛

- [x] **K1**：依赖锁同步。
  - 验证：`uv sync --locked`。
- [x] **K2**：格式检查通过。
  - 验证：`uv run ruff format --check .`。
- [x] **K3**：Lint通过。
  - 验证：`uv run ruff check .`。
- [x] **K4**：Python编译通过。
  - 验证：`uv run python -m compileall -q src tests`。
- [x] **K5**：完整测试通过。
  - 验证：`uv run pytest -q`，记录实际通过/跳过数。
- [x] **K6**：补丁无空白错误。
  - 验证：`git diff --check`。
- [x] **K7**：Git中没有会话结果、真实配置或密钥。
  - 验证：`git status --short` 和敏感模式扫描。

## L. tmux 真实端到端验收

- [x] **L1：大工具结果落盘**
  - 使用真实 Dragon Code 触发超过50KB的Read/Bash或本地测试MCP结果。
  - TUI只显示预览，预览包含路径。
  - 磁盘文件大小和完整尾部标记正确。
- [x] **L2：模型主动重读**
  - 把预览交给真实 DeepSeek，要求继续完成任务。
  - 模型调用Read读取保存路径，并能引用完整结果尾部信息。
- [x] **L3：自动摘要模型**
  - 临时使用较小 `context_window` 触发自动摘要。
  - 证据证明摘要请求使用 `deepseek-v4-flash`，且没有工具调用。
- [x] **L4：自动摘要后继续任务**
  - TUI依次显示压缩开始与before/after。
  - 主模型在压缩历史基础上继续并完成原请求。
- [x] **L5：低Token手动压缩**
  - 新会话历史较短时输入 `/compact`。
  - 仍发起摘要，命令本身不出现在模型历史。
- [x] **L6：熔断与手动绕过**
  - 使用可控失败摘要端点/fake运行入口连续失败3次。
  - 自动路径停止，手动路径仍执行；如果真实端点无法稳定制造错误，在报告中明确标为自动化证据，不伪装tmux实测。
- [x] **L7：退出清理与目录保留**
  - `/exit` 后无残留Dragon Code/MCP测试子进程。
  - 本次 `.dragon-code/sessions/<id>/tool-results/` 仍存在。

## M. 文档、交接和Git

- [x] **M1**：`acceptance-report.md` 记录K组命令的真实输出和L组证据。
- [x] **M2**：`docs/PROJECT_HANDOFF.md` 更新ch08能力、边界、测试结果、tmux证据和下一步。
- [x] **M3**：Spec、Plan、Task与实际实现一致；设计变化已先回写文档并获批准。
- [x] **M4**：只暂存ch08范围文件。
- [x] **M5**：创建本地提交，提交信息准确；没有用户明确要求时不push。
- [x] **M6**：`git show --stat --oneline HEAD` 中无API Key、会话结果或无关文件。
