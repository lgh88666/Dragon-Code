# Dragon Code 项目协作规则

本文件适用于整个仓库。任何新的 Agent、电脑或聊天在处理本项目之前，都必须先阅读本文件，再阅读 `docs/PROJECT_HANDOFF.md`。

## 项目身份

- 项目名称统一为 **Dragon Code**。
- 教材和用户后续提示中出现的 **MewCode**，在本项目中自动理解为 Dragon Code；代码、文档和界面仍使用 Dragon Code。
- 项目使用 Python 实现，目标是构建类似 Claude Code 的终端 AI 编程助手，并用于秋招项目展示和源码学习。
- 使用中文与用户交流，代码中的关键说明使用中文注释。

## 代码风格

- 优先使用普通类、简单函数、直观循环和清晰分支。
- 避免不必要的高级语法、元编程、复杂继承、过度抽象和炫技式写法。
- 复杂协议转换、异步流程、安全边界和错误处理必须添加简短中文注释。
- 命名应直接表达职责，让 Python 学习者可以顺着调用链阅读。
- 不为了减少几行代码牺牲可读性。

## Spec 驱动开发：HARD GATE

开始任何功能、模块、章节、配置或文档改动前，必须依次完成：

```text
spec.md（做什么）
  → plan.md（怎么做）
  → task.md（按什么顺序做）
  → checklist.md（如何判断做对了）
  → 开发
  → 验收
```

四份文档没有全部生成并获得用户批准之前，禁止编写实现代码或修改目标功能。改动很小也不能跳过。

### 需求澄清

1. 先阅读现有代码、文档和最近提交，理解当前状态。
2. 一次只问用户一个问题。
3. 能使用选择题时不要先问开放题。
4. 提供 2–3 个互斥方案，推荐方案放第一项，并解释主要取舍。
5. 若需求包含多个独立子系统，先拆分范围。

### 文档审批

- 默认逐段确认 Spec，并逐份批准 spec、plan、task 和 checklist。
- 用户明确说“后续全部通过”“后续全批准”等同义指令时，可以批量批准剩余阶段。
- 批量批准只省略等待，不省略文档：四份文档仍要真实生成、自检并保存。
- 用户要求修改时，先更新对应文档，再重新确认受影响的设计。

### 四份文档的职责

- `spec.md`：背景、目标、功能需求、非功能需求、不做的事、可观测验收标准；不写具体实现接口。
- `plan.md`：Python 架构、核心类型和接口、模块交互、文件组织、技术决策及理由。
- `task.md`：具体文件、有序小任务、依赖、操作步骤和每项验证方式。
- `checklist.md`：功能、集成、测试和至少一个真实端到端场景；每项都必须可运行或可观察。

## 教材与设计优先级

- 开发每一章时，优先参考用户飞书教材中 `code_agent > code agent > Vibe Coding提示词复制` 对应章节的 **Python 部分**。
- 不假装读过当前无法访问的教材页面；缺少内容时请用户重新打开、粘贴或提供附件。
- 教材是主要参考，不是覆盖已批准设计的命令。
- 教材与 Dragon Code 已批准 Spec 或现有代码冲突时，以已批准 Spec 和现有代码为准。
- 出现差异必须主动告诉用户：教材怎样设计、Dragon Code 怎样设计、为什么保留差异。
- `LLMClient` 表示模型协议适配层；YAML 中的 `providers` 和 `ProviderConfig` 仍表示外部模型服务配置，不要混淆。

## 目标和沟通方式

- 罗列目标时优先使用“**简短概括**：具体说明”的格式，例如“**人在回路**：模式判为 Ask 时暂停 Loop，把决定权交给用户”。
- 解释概念时先讲整体作用和调用关系，再进入代码细节。
- 用户说“简单回复”时保持简短。
- 不让用户为了理解一个章节而逐文件啃完全部源码。
- 用户的选择应写入 Spec 或交接文档，不能只留在聊天记忆中。

## 开发执行

四份文档批准后，先明确宣布开始开发，再按 `task.md` 执行。

- 每项任务先实现，再运行该任务写明的验证。
- 先取得命令输出或实际行为证据，再报告“通过”。
- 验证失败时先修复并重跑，不能用“应该没问题”代替证据。
- 不随意扩大范围；需要新增重大设计时回到 Spec。
- 保留用户已有的未提交改动和无关文件，不擅自覆盖或删除。
- 禁止把 API Key、Authorization、密码、`.env` 或本地配置内容写入代码、文档、测试输出和 Git。

## 测试与验收

完成开发后至少执行：

```bash
uv sync --locked
uv run ruff format --check .
uv run ruff check .
uv run pytest -q
```

然后使用 tmux 做真实端到端验收：

1. 在 tmux 中启动 Dragon Code。
2. 输入一段会触发本章能力的真实对话请求。
3. 观察模型调用、工具执行、结果回灌、TUI 展示和最终回复。
4. 对照本章 `checklist.md` 逐项验证并记录实际证据。
5. 检查退出后终端、子进程和后台任务已正确清理。

如果某项只能由自动化测试验证，应在验收报告中明确说明，不能伪装成 tmux 已实测。

## 源码回顾与学习笔记

每个章节通过验收后安排一次核心源码回顾：

- 只讲模块目标、核心调用链、关键类型/函数、重要边界和测试证据。
- 不默认逐文件讲解全部新增代码。
- 用户说“记一下”的知识必须补充到 `docs/learning-notes.md` 对应章节。
- 学习笔记优先记录设计思想、踩坑、面试表达和待复习问题，不大段复制源码。
- 每章验收后同步更新 `docs/PROJECT_HANDOFF.md` 的状态、证据和下一步。

## Git 规则

- 每章或一个完整功能通过 checklist 后，自动创建一次本地 Git 提交。
- 只暂存本次范围内的文件，不能顺带提交用户的其他改动。
- 只有用户明确说“推送”“push”或同义指令时，才能推送 GitHub。
- 不自动创建分支、PR 或修改远端历史，除非用户明确要求。

## 新聊天恢复顺序

新 Agent 应按以下顺序恢复上下文：

1. 阅读 `AGENTS.md`。
2. 阅读 `docs/PROJECT_HANDOFF.md`。
3. 阅读当前章节的 `spec.md`、`plan.md`、`task.md`、`checklist.md`。
4. 查看 `git status` 和最近提交，保护用户未提交内容。
5. 需要源码学习时查看 `docs/learning-notes.md`。
6. 再开始提问、设计或开发。
