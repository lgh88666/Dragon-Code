# Dragon Code 多协议 LLM 终端对话客户端 Checklist

> 每一项都通过运行程序、自动测试或观察实际行为验证。验收时记录命令输出、tmux
> `capture-pane` 文本或终端截图作为证据。

## 实现完整性

- [ ] **配置加载与单 Provider 直进（AC1/F1）**  
  验证：使用一条合法 Provider 配置启动，直接看到对话界面；依次测试配置文件缺失、
  YAML 错误、密钥缺失，均看到可读错误、非零退出码且无 Python 堆栈。

- [ ] **多 Provider 选择（AC2/F2）**  
  验证：配置两条 Provider，启动后看到方向键列表；选择第二项后进入对话，状态栏显示
  第二项的名称和模型。

- [ ] **Anthropic 与 OpenAI 协议适配（AC3/F3/N3）**  
  验证：两种协议分别完成同一条真实对话；再为 OpenAI 兼容服务配置自定义 `base_url`
  完成一轮请求。两种协议的上层交互方式一致。

- [ ] **System Prompt、历史与 thinking 请求参数（AC4/F4）**  
  验证：自动测试捕获两个适配器的请求参数，确认包含内置 System Prompt 和完整消息历史；
  Anthropic 配置 `thinking: true` 时包含合法思考参数；OpenAI Chat Completions 不传该参数。

- [ ] **流式正文与思考过滤（AC5/F5）**  
  验证：真实长回复可见逐步输出；Anthropic 开启 thinking 后，界面和最终历史中均不出现
  思考文本。

- [ ] **单会话多轮上下文（AC6/F6）**  
  验证：第一轮告诉模型一个随机短语，第二轮询问该短语，模型正确回答；退出重启后再次
  询问，模型不知道之前内容。

- [ ] **完整 TUI 布局（AC7/F7）**  
  验证：启动截图中同时包含 ASCII 猫、Dragon Code 名称和版本、当前工作目录、就绪提示、
  对话区、带 `❯` 和占位符的输入框，以及左侧 Provider 名和右侧模型名。

- [ ] **流式结束后的 Markdown 定型（AC8/F8）**  
  验证：要求模型输出标题、强调、列表和 Python 代码块；流式期间显示纯文本，结束后四种
  Markdown 格式均正确渲染。

- [ ] **多行输入与提交（AC9/F9）**  
  验证：输入第一行，按 Alt+Enter 插入换行，再输入第二行；按 Enter 后只提交一次且输入框
  清空，模型收到两行内容。

- [ ] **安全退出（AC10/F10/N7）**  
  验证：分别通过 `/exit` 和 Ctrl+C 退出；回到 shell 后输入普通命令，终端回显、光标和
  换行均正常。

- [ ] **运行时错误恢复（AC11/F11）**  
  验证：使用错误密钥或不存在模型发起请求，看到可区分的错误样式；应用不退出，输入框
  恢复可用并能再次提交。Pilot 测试另外验证“第一次失败、第二次成功”。

- [ ] **响应计时（AC12/F12）**  
  验证：提交后、首个正文到达前即可看到 `Imagining… (Ns)`；秒数持续增加，完成后显示
  本轮总耗时。

- [ ] **等待和流式期间不冻结（AC13/N1/N2）**  
  验证：慢速回复期间计时持续变化，对话区可滚动，窗口调整后界面继续刷新；不能出现
  整个界面停止响应。

## 集成与健壮性

- [x] **统一 Provider 行为**  
  验证：TUI 和 ChatSession 只使用统一 Provider 接口；Anthropic/OpenAI 适配器自动测试
  均产生相同形式的文本流。
  证据：`test_provider_anthropic.py`、`test_provider_openai.py` 与 `test_session.py` 通过。

- [x] **成功后才提交历史**  
  验证：成功请求后历史增加一组 user/assistant 消息；失败请求后历史长度不变，下一次
  请求不携带失败轮次。
  证据：`test_session_success_commits_history`、`test_session_failure_does_not_commit_history`
  和多轮历史测试通过。

- [x] **错误分类与脱敏**  
  验证：鉴权、限流、网络、模型不存在、参数和未知错误均转换为公开错误；测试异常中放入
  假密钥，所有输出和错误文本均搜索不到该值。
  证据：Provider 错误测试覆盖全部分类，假密钥未出现在公开错误中。

- [x] **配置边界完整**  
  验证：providers 为空、条目不是对象、字段为空、protocol 非法、`thinking` 类型错误均
  得到包含字段位置的可读错误。
  证据：配置测试包含上述边界，缺配置入口返回退出码 1 且无堆栈。

- [ ] **自定义端点生效**  
  验证：自动测试确认 `base_url` 传给对应 SDK；真实兼容端点配置能完成一轮对话。

- [x] **输入并发保护**  
  验证：流式期间再次按 Enter 不会启动第二个请求；当前流完成后输入恢复。
  证据：Textual Pilot 的并发提交测试确认 Provider 只调用一次。

- [ ] **窄屏与尺寸变化（N6）**  
  验证：在 tmux 中缩窄和恢复窗格宽度，Banner、对话、Markdown、输入框和状态栏没有重叠，
  主要内容仍可阅读。
  证据：Pilot 已在 42×18 终端验证主要 Widget；tmux 真实缩放仍待执行。

- [x] **密钥不进入版本控制或输出（N5）**  
  验证：`.dragon-code/config.yaml` 被 Git 忽略；`git diff`、程序输出、测试输出和对象打印
  中均无真实 API Key。
  证据：`git check-ignore` 命中真实配置；对象 repr 和错误脱敏测试通过；仓库范围检索无
  本地冒烟测试密钥。

- [x] **范围边界未被突破**  
  验证：本阶段没有工具调用、MCP、会话持久化、上下文压缩、自动重试、模型运行时切换或
  `/exit` 以外的 slash 命令。
  证据：源码检索未发现这些实现；README 明确列为本章边界。

## 安装、格式与自动测试

- [x] `uv sync --locked` 成功，依赖与 `uv.lock` 一致（36 个包检查通过）。
- [x] `uv run ruff format --check .` 返回退出码 0（25 个文件已格式化）。
- [x] `uv run ruff check .` 返回退出码 0。
- [x] `uv run pytest -q` 返回退出码 0（37 项通过，无跳过）。
- [x] `uv run python -m dragon_code` 在合法配置下进入 TUI（持续运行至冒烟超时终止）。
- [x] `uv run dragon-code` 与模块入口行为一致（同样进入持续运行的 TUI）。
- [ ] `README.md` 中的安装、配置、运行和快捷键步骤可从头执行。

## tmux 端到端场景

### 场景 1：Anthropic 多轮、流式与 Markdown

- [ ] 在 WSL/Linux 中创建 tmux 会话并启动 Dragon Code。
- [ ] 使用真实 Anthropic 配置且开启 thinking。
- [ ] 第一轮输入：`请记住暗号“青龙七号”，并用一句话确认。`
- [ ] 第二轮输入：`刚才的暗号是什么？请用标题、列表和 Python 代码块回答。`
- [ ] 观察到流式正文、实时计时、正确引用“青龙七号”、Markdown 定型，且无 thinking 内容。
- [ ] 使用 `tmux capture-pane -p -S -` 或截图保存证据。

### 场景 2：OpenAI 协议一致体验

- [ ] 使用真实 OpenAI 或 OpenAI 兼容端点配置，在 tmux 中启动。
- [ ] 输入：`请分三点解释 Python async/await，并给出一个简短代码块。`
- [ ] 观察到与 Anthropic 一致的提交、流式输出、计时、Markdown 和状态栏体验。
- [ ] 使用 `tmux capture-pane` 或截图保存证据。

### 场景 3：多 Provider 选择

- [ ] 配置至少两个 Provider，在 tmux 中启动。
- [ ] 观察到选择列表，用方向键选中第二项并按 Enter。
- [ ] 状态栏显示第二项的名称和模型，随后真实对话成功。

### 场景 4：多行输入与界面响应

- [ ] 使用 Alt+Enter 输入两行真实请求，再按 Enter 提交。
- [ ] 流式期间滚动对话区并调整 tmux 窗格宽度。
- [ ] 观察到只发起一个请求、计时不中断、界面不冻结，完成后 Markdown 正常。

### 场景 5：错误恢复

- [ ] 使用错误密钥或不存在的模型启动并提交请求。
- [ ] 观察到可区分的错误信息，应用没有退出，输入框恢复可用。
- [ ] 再提交一条消息，确认会话仍能处理输入且没有崩溃堆栈或密钥泄露。

### 场景 6：退出与终端恢复

- [ ] 在一轮空闲状态输入 `/exit`，回到正常 shell。
- [ ] 重新启动后按 Ctrl+C，回到正常 shell。
- [ ] 两次退出后分别执行 `printf 'terminal-ok\n'`，均正常显示。

## 验收记录格式

验收时为每项填写实际结果：

```text
- [x] 条目名称
  证据：运行了什么命令或输入了什么内容
  实际：观察到的输出、退出码或界面行为
```

如有失败，保留未勾选状态并记录：

```text
- [ ] 条目名称
  预期：应看到什么
  实际：实际看到了什么
  修复：准备如何修复以及重测方式
```
